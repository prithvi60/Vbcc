export const metadata = {
  title: "VBCC Products",
  description: "Vbbc All Products Pages",
};

export default function RootLayout({ children }) {
  return (
    <main>
      {children}
    </main>
  );
}
