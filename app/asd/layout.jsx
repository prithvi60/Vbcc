export const metadata = {
  title: "VBCC Contact",
  description: "Vbbc Thank You Page",
};

export default function RootLayout({ children }) {
  return (
    <main>
      {children}
    </main>
  );
}
