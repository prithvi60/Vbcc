export const metadata = {
  title: "VBCC Contact",
  description: "Vbbc Contact Us Pages",
};

export default function RootLayout({ children }) {
  return (
    <main>
      {children}
    </main>
  );
}
