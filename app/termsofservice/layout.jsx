export const metadata = {
  title: "VBCC Contact",
  description: "Vbbc Terms Of Service Page",
};

export default function RootLayout({ children }) {
  return (
    <main>
      {children}
    </main>
  );
}
