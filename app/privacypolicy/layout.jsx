export const metadata = {
  title: "VBCC Contact",
  description: "Vbbc privacy PolicyPage",
};

export default function RootLayout({ children }) {
  return (
    <main>
      {children}
    </main>
  );
}
