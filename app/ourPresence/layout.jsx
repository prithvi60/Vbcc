export const metadata = {
  title: "VBCC Our Presence",
  description: "Vbbc Our Presence Page",
};

export default function RootLayout({ children }) {
  return (
    <main>
      {children}
    </main>
  );
}
